{
  "name": "@yousan/show-version",
  "version": "1.5.7",
  "description": "A CLI tool to display version information from package.json",
  "main": "dist/index.js",
  "bin": {
    "show-version": "./dist/bin/show-version.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs:build": "jsdoc -c jsdoc.json",
    "docs:serve": "serve docs",
    "build": "tsc",
    "watch": "tsc -w",
    "prepare": "npm run build && cp package.json dist/ && cp -r node_modules dist/"
  },
  "keywords": [
    "version",
    "cli",
    "package.json"
  ],
  "author": "yousan",
  "license": "MIT",
  "dependencies": {
    "commander": "^11.1.0",
    "isomorphic-git": "^1.25.6"
  },
  "devDependencies": {
    "@types/node": "^20.17.30",
    "@types/react": "^18.2.61",
    "@types/react-dom": "^18.2.19",
    "jsdoc": "^4.0.2",
    "serve": "^14.2.1",
    "typescript": "^5.3.3"
  }
}
